

class AUTH_GROUP(Base):
    __tablename__ = 'AUTH_GROUP'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    name = Column(VARCHAR(150), nullable=False)


class AUTH_GROUP_PERMISSIONS(Base):
    __tablename__ = 'AUTH_GROUP_PERMISSIONS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    group_id = Column(INTEGER(), nullable=False)
    permission_id = Column(INTEGER(), nullable=False)


class AUTH_PERMISSION(Base):
    __tablename__ = 'AUTH_PERMISSION'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    name = Column(VARCHAR(255), nullable=False)
    content_type_id = Column(INTEGER(), nullable=False)
    codename = Column(VARCHAR(100), nullable=False)


class AUTH_USER(Base):
    __tablename__ = 'AUTH_USER'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    password = Column(VARCHAR(128), nullable=False)
    last_login = Column(DATETIME(), nullable=True)
    is_superuser = Column(TINYINT(), nullable=False)
    username = Column(VARCHAR(150), nullable=False)
    first_name = Column(VARCHAR(150), nullable=False)
    last_name = Column(VARCHAR(150), nullable=False)
    email = Column(VARCHAR(254), nullable=False)
    is_staff = Column(TINYINT(), nullable=False)
    is_active = Column(TINYINT(), nullable=False)
    date_joined = Column(DATETIME(), nullable=False)


class AUTH_USER_GROUPS(Base):
    __tablename__ = 'AUTH_USER_GROUPS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    user_id = Column(INTEGER(), nullable=False)
    group_id = Column(INTEGER(), nullable=False)


class AUTH_USER_USER_PERMISSIONS(Base):
    __tablename__ = 'AUTH_USER_USER_PERMISSIONS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    user_id = Column(INTEGER(), nullable=False)
    permission_id = Column(INTEGER(), nullable=False)


class DJANGO_ADMIN_LOG(Base):
    __tablename__ = 'DJANGO_ADMIN_LOG'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    action_time = Column(DATETIME(), nullable=False)
    object_id = Column(TEXT(), nullable=True)
    object_repr = Column(VARCHAR(200), nullable=False)
    action_flag = Column(INTEGER(unsigned=True), nullable=False)
    change_message = Column(TEXT(), nullable=False)
    content_type_id = Column(INTEGER(), nullable=True)
    user_id = Column(INTEGER(), nullable=False)


class DJANGO_CONTENT_TYPE(Base):
    __tablename__ = 'DJANGO_CONTENT_TYPE'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    app_label = Column(VARCHAR(100), nullable=False)
    model = Column(VARCHAR(100), nullable=False)


class DJANGO_MIGRATIONS(Base):
    __tablename__ = 'DJANGO_MIGRATIONS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    app = Column(VARCHAR(255), nullable=False)
    name = Column(VARCHAR(255), nullable=False)
    applied = Column(DATETIME(), nullable=False)


class DJANGO_SESSION(Base):
    __tablename__ = 'DJANGO_SESSION'

    session_key = Column(VARCHAR(40),  primary_key=True, nullable=False)
    session_data = Column(TEXT(), nullable=False)
    expire_date = Column(DATETIME(), nullable=False)


class ITEMS(Base):
    __tablename__ = 'ITEMS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    name = Column(TEXT(), nullable=False)
    description = Column(TEXT(), nullable=False)
    price_m = Column(INTEGER(), nullable=False)
    price_l = Column(INTEGER(), nullable=False)
    image_path = Column(TEXT(), nullable=False)
    deleted = Column(TINYINT(), nullable=False)


class ORDER_ITEMS(Base):
    __tablename__ = 'ORDER_ITEMS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    quantity = Column(INTEGER(), nullable=False)
    size = Column(VARCHAR(1), nullable=False)
    item_id = Column(INTEGER(), nullable=False)
    order_id = Column(INTEGER(), nullable=False)


class ORDER_TOPPINGS(Base):
    __tablename__ = 'ORDER_TOPPINGS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    order_item_id = Column(INTEGER(), nullable=False)
    topping_id = Column(INTEGER(), nullable=False)


class ORDERS(Base):
    __tablename__ = 'ORDERS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    status = Column(INTEGER(), nullable=False)
    total_price = Column(INTEGER(), nullable=False)
    order_data = Column(, nullable=False)
    destination_name = Column(VARCHAR(100), nullable=False)
    destination_email = Column(VARCHAR(100), nullable=False)
    destination_zipcode = Column(VARCHAR(7), nullable=False)
    destination_address = Column(VARCHAR(200), nullable=False)
    destination_tel = Column(VARCHAR(15), nullable=False)
    delivery_time = Column(DATETIME(), nullable=False)
    payment_method = Column(INTEGER(), nullable=False)
    user_id = Column(INTEGER(), nullable=False)


class TOPPINGS(Base):
    __tablename__ = 'TOPPINGS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    name = Column(TEXT(), nullable=False)
    price_m = Column(INTEGER(), nullable=False)
    price_l = Column(INTEGER(), nullable=False)


class USER_UTILS(Base):
    __tablename__ = 'USER_UTILS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    token = Column(TEXT(), nullable=False)
    created_at = Column(DATETIME(), nullable=False)
    user_id = Column(INTEGER(), nullable=False)


class USERS(Base):
    __tablename__ = 'USERS'

    id = Column(INTEGER(),  primary_key=True, nullable=False)
    name = Column(VARCHAR(100), nullable=False)
    email = Column(VARCHAR(100), nullable=False)
    password = Column(TEXT(), nullable=False)
    zipcode = Column(VARCHAR(7), nullable=False)
    address = Column(VARCHAR(200), nullable=False)
    telephone = Column(VARCHAR(15), nullable=False)
    status = Column(VARCHAR(1), nullable=False)
