version: "3.3"

services:
  django:
    build: ./python
    image: django
    container_name: django
    volumes:
      - ./src:/code
      - ./python/uwsgi/:/etc/uwsgi/
    expose:
      - "8000"
    external_links:
      - db
      - nginx

    networks:
      - common_link

networks:
  common_link:
    external: true
